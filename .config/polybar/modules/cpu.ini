[module/cpu]
type = internal/cpu
interval = 2

format = %{A1:notify-send "Temp" "$(sensors acpitz-acpi-0| grep -A 0 'temp1' | cut -c16-17)°C":} <label>%{A}
format-foreground = ${colors.blue}

label = %{T2}󰘚 %{T-}%percentage%%
